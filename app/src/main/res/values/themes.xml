<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.IndigoRx" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary -->
        <item name="colorPrimary">@color/indigo_primary</item>
        <item name="colorPrimaryContainer">@color/indigo_light</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnPrimaryContainer">@color/indigo_dark</item>

        <!-- Secondary -->
        <item name="colorSecondary">@color/indigo_accent</item>
        <item name="colorSecondaryContainer">@color/indigo_light</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorOnSecondaryContainer">@color/indigo_dark</item>

        <!-- Surface -->
        <item name="colorSurface">@color/surface</item>
        <item name="colorSurfaceVariant">@color/surface_variant</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="colorOnSurfaceVariant">@color/text_secondary</item>

        <!-- Background -->
        <item name="android:colorBackground">@color/background</item>

        <!-- Outline -->
        <item name="colorOutline">@color/divider</item>
        <item name="colorOutlineVariant">@color/divider</item>

        <!-- Error -->
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/white</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">?android:colorBackground</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Navigation Bar -->
        <item name="android:navigationBarColor">?android:colorBackground</item>
        <item name="android:windowLightNavigationBar">true</item>

        <!-- Styles -->
        <item name="textInputStyle">@style/IndigoRx.TextInputLayout</item>
        <item name="materialButtonStyle">@style/IndigoRx.Button</item>
    </style>

    <!-- TextInputLayout Style -->
    <style name="IndigoRx.TextInputLayout" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxCornerRadiusTopStart">12dp</item>
        <item name="boxCornerRadiusTopEnd">12dp</item>
        <item name="boxCornerRadiusBottomStart">12dp</item>
        <item name="boxCornerRadiusBottomEnd">12dp</item>
        <item name="hintTextColor">?colorPrimary</item>
        <item name="boxStrokeColor">@color/text_input_box_stroke</item>
    </style>

    <!-- Button Style -->
    <style name="IndigoRx.Button" parent="Widget.Material3.Button">
        <item name="cornerRadius">12dp</item>
        <item name="backgroundTint">@color/indigo_primary</item>

    </style>

</resources>